#!/usr/bin/env python
# encoding: utf-8

from setuptools import setup, find_packages

setup(
    name="flask_servatus",
    version="0.0.1",
    author='Old Street Labs',
    author_email='developers@oldstlabs.com',
    packages=find_packages(exclude=[])
)
